'use client';

// No default demo user account - users must connect their own accounts
export const mockUser = null;

// Empty email list by default (no demo emails)
export const mockEmails = [];

// Empty notifications list by default
export const mockNotifications = [];


