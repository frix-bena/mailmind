'use client';

// User structure
export const mockUser = null;

// Empty email collection - real emails are loaded dynamically via IMAP
export const mockEmails = [];

// Empty notifications - real notifications are generated dynamically
export const mockNotifications = [];

